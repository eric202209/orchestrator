[project]
name = "amd-tiny-source-rewrite-fixture"
version = "0.1.0"
description = "Tiny existing-source rewrite fixture for AMD local model lane testing"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
