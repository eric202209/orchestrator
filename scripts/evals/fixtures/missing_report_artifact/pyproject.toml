[project]
name = "missing-report-artifact-fixture"
version = "0.1.0"
description = "Seed fixture for orchestrator missing_report_artifact evaluation"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
