id: python-bug-fix
display_name: Python Bug Fix
workflow_profile: debug_only
review_policy:
  auto_promote_if: [no_deletes, no_dependency_changes]
  hold_if: [has_deletes, config_change]
allowed_ops: [write_file, replace_in_file, append_file, read_file, shell_command]
verification: mutation
auto_promote_eligible: true
risk_flags: [modifies_tests, modifies_config]
