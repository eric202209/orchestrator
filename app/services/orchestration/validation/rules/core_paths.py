"""Reserved for core_invariant path-safety rules (Phase 20J skeleton, empty).

Will own workspace-escape and path-boundary checks (e.g. rejecting file
operations outside the task workspace) that must hold regardless of
workload. No rule implementation has moved here yet.
"""

from __future__ import annotations
